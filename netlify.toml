[build]
  command = "npm install && npm run build"
  functions = "functions"

 [[redirects]]
   from = "/*"
   to = "https://slack.christopherwillard.com/.netlify/functions/server/:splat"
   status = 200
